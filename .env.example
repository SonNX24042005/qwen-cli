# Token đăng nhập Qwen Chat Web (chat.qwen.ai).
# Có thể để trống — ở lần chạy đầu tiên, CLI sẽ tự mở trình duyệt để bạn đăng nhập
# và tự động điền token vào file .env (sao chép từ .env.example này) giúp bạn.
QWEN_TOKEN=
